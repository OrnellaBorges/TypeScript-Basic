interface Point {

    x:number

}

interface Point {

    y:number

}

// les interfaces s'ecrivent comme des classes sont bcp moins flexlibles que les type
// elles ne se base pas sur des types primitives 
// elles restent ouvertes : on peut modifier plus tar a tout moment dans le script = la fusion

const p: Point ; // auras x et y 